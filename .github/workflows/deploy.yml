name: Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Deployment target environment
        required: true
        type: choice
        options:
          - staging
          - production

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deployment placeholder
        run: |
          echo "Preparing deployment to ${{ inputs.environment }}"
          echo "TODO: add deployment commands and credentials for ${{ inputs.environment }}"
